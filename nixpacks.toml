[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r new/requirements.txt"]

[start]
cmd = "cd new && uvicorn src.main:app --host 0.0.0.0 --port $PORT"
```

### Option B: Create `runtime.txt` in root
```
python-3.11.0